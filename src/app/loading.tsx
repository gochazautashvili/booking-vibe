const loading = () => {
  return (
    <div className="sticky top-0 left-0 right-0 w-full h-screen bg-primary/80 flex items-center justify-center text-white text-3xl font-semibold">
      Loading...
    </div>
  );
};

export default loading;
